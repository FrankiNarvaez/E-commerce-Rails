<section class="category">
  <%= category.name %>
  <%= link_to t('.edit'), edit_category_path(category.id) %>
  <%= link_to t('.delete'), category_path(category.id), data: {
    turbo_method: :delete,
    turbo_confirm: t('common.confirm')
  } %>
</section>
