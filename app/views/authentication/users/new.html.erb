<div class="flex flex-col justify-center items-center w-screen h-screen">
  <h2 class="text-main-color text-5xl font-bold mb-4"><%= t('.title') %></h2>

  <%= render "form" %>

  <div class="flex gap-1 mt-4">
    <p><%= t('.have_account') %></p>
    <%= link_to t(".link_sign_in"), new_session_path, class: "font-semibold text-main-color" %>
  </div>
</div>
